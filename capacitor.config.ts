import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'doctrinal-mastery-restart-2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
